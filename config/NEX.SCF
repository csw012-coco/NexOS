# NexOS config
#
# Paths are relative to the mounted root unless they start with '/'.
# The kernel reads boot keys; userland tools may read their own keys.
# Set this to enable explicit root capability requests:
security.root_token=what

ring3_smoke=0
mouse.cursor=0
serial_shell=off
virtual_tty_shells=on
function_recursion_limit=0
