NexOS i386 maintenance/refactor TODO
====================================

1. test32 VM query helper cleanup
   - Add helpers:
     - test32_vm_query_or_fail(struct syscall_vm_info *out)
     - test32_vm_same_mapping_counts(a, b)
     - test32_vm_no_mapping_growth(before, after)
   - Use them in:
     - user/i386/test32_proc.c
       - fork-cow-cleanup
       - fork-cow-ownership
       - fork-mmap-exec
       - exec-fail-cleanup
     - user/i386/test32_mm.c
       - mmap_fault_cleanup
       - mmap_kill_cleanup
       - shared_fault_cleanup
       - invalid_pointer_cleanup
       - shm_lifecycle
   - Goal: keep MM tests readable and make mapping-count checks express intent directly.

2. Split strict MM checks from full smoke checks
   - Keep check-i386-nexbox32-full as a stable functional smoke.
   - Add an explicit strict path for refcount/page/mapping-count assertions.
   - Possible commands:
     - /BOOT/TEST32.ELF strict-mm
     - /BOOT/TEST32.ELF live-shm
     - /BOOT/TEST32.ELF fork-cow-strict
   - Goal: smoke stays stable while deep MM regressions are still testable on demand.

3. Finish test32 helper pass
   - Keep already-added helpers:
     - TEST32_ELF_PATH
     - test32_make_unique_name()
     - test32_cleanup_mmap()
     - test32_cleanup_shm()
     - test32_cleanup_mmap_shm()
     - test32_spawn_background()
     - test32_wait_expect()
     - test32_spawn_wait_expect()
   - Continue only where helper use improves readability.
   - Do not hide important test actions like kill(), fg/bg, wait-info validation, or exec_replace.

4. i386 syscall adapter thinness audit
   - syscall_i386_request_adapter.c should keep:
     - int 0x40 register decode
     - 32-bit pointer validation
     - copy_to_user/copy_from_user/copy_user_cstr ABI helpers
     - request/result bridge into common syscall core
   - Move or keep moved out:
     - mmap/shm/mq/sem dispatch
     - audio/gfx/block/gui/clipboard dispatch
     - query/event/power pure handlers
   - Goal: i386 adapter is an ABI adapter, not a syscall implementation hub.

5. process32/scheduler thin backend audit
   - process32.c target shape:
     - ELF32 loader/backend
     - i386 command adapter
     - arch-specific user entry preparation
   - scheduler.c target shape:
     - timer tick glue
     - current task slot bridge
     - context switch/FPU/CR3 arch state
   - Move remaining lifecycle policy into common process/scheduler helpers where practical.

6. i386 shared services residue audit
   - i386_shared_services.c should keep only:
     - i386 boot/service glue
     - arch-specific service registration
   - Keep boot smoke runners, TTY selftests, command runners, driver smoke, and autostart glue split by responsibility.

7. Driver/backend parity follow-up
   - Keep checking active paths for:
     - AHCI read/write/sync/flush
     - EHCI/XHCI + USB MSC
     - USB HID
     - RTL8139 TX/RX
     - AC97/HDA audio
   - Ensure /proc/drivers, drivers, doctor --table use the same state/reason model on i386 and x86_64.

8. Userland parity follow-up
   - Expand NEXBOX32 full applet smoke only for commands that are stable.
   - Continue checking:
     - storage/proc/gfx/audio/net/doctor/editor applets
     - UTF-8/Unifont/Hangul input/editing parity
     - clipboard behavior
     - gfx/editor/doom runtime smoke

Regression commands to keep green
=================================

- make CCACHE= ARCH=i386 check
- make CCACHE= check-i386-nexbox32-full
- make CCACHE= check-x86_64-nexbox-full

=====
